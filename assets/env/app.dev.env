# Development flavor — merged after app.env.example.
# Use for local-only toggles; keep secrets out of git (use app.env or dart-define).
# SUPABASE_URL / SUPABASE_ANON_KEY default to the local `supabase start` stack
# (see EnvConfig). Point them at the cloud dev project here once it exists.
