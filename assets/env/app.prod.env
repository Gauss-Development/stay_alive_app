# Production flavor — merged after app.env.example.
# Inject real RevenueCat SDK keys and Appwrite production IDs in CI using
# --dart-define-from-file or individual --dart-define flags (they override .env).

APPWRITE_SELF_SIGNED=false
