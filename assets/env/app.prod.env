# Production flavor — merged after app.env.example.
# SUPABASE_URL / SUPABASE_ANON_KEY intentionally stay empty here: production
# refuses the committed dev fallbacks and bootstrap throws unless real values
# are injected in CI via --dart-define (see .github/workflows/release.yml).
