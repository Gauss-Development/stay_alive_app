export default async ({ req, res, log, error }) => {
  log('daily_rollup stub: deploy and implement server-side streak denormalization');
  return res.json({
    ok: true,
    message: 'daily_rollup stub — see README.md for contract',
  });
};
